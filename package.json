{
  "name": "restful-api",
  "version": "1.0.0",
  "description": "A fake RESTful API for testing purposes, ready to deploy to services like Heroku.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nbkhope",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.9.1"
  }
}
